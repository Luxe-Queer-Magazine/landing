{
  "version": 2,
  "builds": [
    { "src": "out/**", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/out/$1" },
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/out/index.html" }
  ]
}
